extends ./layouts/skeleton.pug

block content
    .uk-section
        .uk-container
            div(class="uk-child-width-1-3@s uk-text-center" uk-grid)
                each val, index in ['zero', 'one', 'two']
                    div
                        .uk-card.uk-card-body.uk-card-default= val
    